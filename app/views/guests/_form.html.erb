<%= simple_form_for(@guest) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :email %>
  <br>
  <%= f.input :plussone, :as => :boolean, label: "+1 guest" %>
  <br>
  <%= f.association :dinners, as: :check_boxes %>
  <%= f.button :submit %>
<% end %>
